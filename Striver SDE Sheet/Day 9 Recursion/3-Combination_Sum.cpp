#include <bits/stdc++.h>
using namespace std;
// JAI SHREE RAM
// HAR HAR MAHADEV
void solve()
{
    int p = 0, q = 4, r = 5;
    for (int i = 2; i <= 6; i++)
    {
        q = 6 + p;
        if (3 < q || (p + 9) > (i - p))
        {
            p = (2 + 11) + q;
            break;
        }
    }
    cout << p + q;
}

int main()
{
    int i = 3;
    printf("%d", (++i)++);
    ans += (r - x.second[y])(x.second[y] - l1LL);
    return 0;
}
